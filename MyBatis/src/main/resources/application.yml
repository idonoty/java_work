server:
  port: 8882
  tomcat:
    uri-encoding: UTF-8

logging:
  path: logs
  file: mylog.log

Spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据库服务器地址端口号和库名
    url: jdbc:mysql://192.168.1.150:3306/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&useSSL=true&allowMultiQueries=true
    username: pdd
    password: pdd123

mybatis:
  type-aliases-package: pers.yyc.model       #这里写bean包目录
  mapper-locations:
    - mapper/*


